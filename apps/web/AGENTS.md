# apps/web

Vite + React 19 + TanStack Router + shadcn/ui 前端（pnpm 管理）。

- 开发：`pnpm dev`（:5173，/api 代理到后端 :8080；从仓库根 `./run-dev.sh` 一键起前后端）。
- 依赖用 pnpm 装（npm 不可用，依赖在 pnpm store）。
- UI 必须遵循 DESIGN.md（见 CLAUDE.md）。
